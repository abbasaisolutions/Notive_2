# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/notive_db?schema=public"

# JWT Secrets (generate strong random strings for production!)
JWT_ACCESS_SECRET="notive_access_secret_change_in_production"
JWT_REFRESH_SECRET="notive_refresh_secret_change_in_production"

# Token Expiry
ACCESS_TOKEN_EXPIRY="15m"
REFRESH_TOKEN_EXPIRY="7d"

# Server Port
PORT=8000

# Public URLs
# API_URL is used to generate some callback URLs (fallbacks to http://localhost:8000/api/v1)
API_URL="http://localhost:8000/api/v1"

# CLIENT_URL is where OAuth callbacks redirect users after connecting
CLIENT_URL="http://localhost:3000"

# Google OAuth (ID token verification for Google Sign-In)
GOOGLE_CLIENT_ID="your_google_client_id_here.apps.googleusercontent.com"

# Instagram Basic Display OAuth
# Set these in your Meta app's "Instagram Basic Display" settings.
INSTAGRAM_CLIENT_ID=""
INSTAGRAM_CLIENT_SECRET=""
INSTAGRAM_REDIRECT_URI="http://localhost:8000/api/v1/imporxt/callback/instagram"

# Facebook Login OAuth
FACEBOOK_APP_ID=""
FACEBOOK_APP_SECRET=""
FACEBOOK_REDIRECT_URI="http://localhost:8000/api/v1/import/callback/facebook"
